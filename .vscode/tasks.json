{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build BetterRandom",
            "type": "shell",
            "command": "node",
            "args": [
                "build.cjs",
            ],
            "options": {
                "cwd": "${workspaceFolder}/src/betterRandom"
            },
            "group": "build",
        },
        {
            "label": "Build DeviceInfo",
            "type": "shell",
            "command": "node",
            "args": [
                "build.cjs",
            ],
            "options": {
                "cwd": "${workspaceFolder}/src/DeviceInfo"
            },
            "group": "build",
        }
    ]
}