{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build BetterRandom",
            "type": "shell",
            "command": "node",
            "args": [
                "build.cjs",
            ],
            "options": {
                "cwd": "${workspaceFolder}/betterRandom"
            },
            "group": "build",
        }
    ]
}